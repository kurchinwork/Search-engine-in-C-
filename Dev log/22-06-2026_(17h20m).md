# Dev Log: 22.06.2026 (17:20)

_______________
Цели на сегодня

Написать тесты для методов нового класса
Дописать метод std::vector<Entry> GetWordCount(const std::string& word);
Добить 4 этап из 7

_______________
Что было сделано

Дописал метод GetResponse по умолчанию возвращает 5
Написал первый метод класса InvertIndex

_______________
Проблемы и решения

Пока не особо понимаю как работает класс InvertIndex
Надо получше разобраться в работе answer и request JSON

Методы Converter JSON работают с записью и чтением из JSON

Коммент от куратора:

По текущему варианту видно, что направление в целом выбрано верно: 
структура проекта есть, CMakeLists.txt есть, подключены JSON и тесты, 
начат ConverterJSON, добавлены конфиги и ресурсы.

Основные вещи ещё нужно реализовать:

1. Добавить класс SearchServer, потому что именно он отвечает за поиск, 
   расчёт релевантности и сортировку результатов.

2. Реализовать InvertedIndex: сейчас методы объявлены, но логики индексации и подсчёта слов пока нет.

3. Доделать ConverterJSON: нужно, чтобы он читал requests.json и записывал результат в answers.json.

4. В GetResponsesLimit() нужно вернуть значение по умолчанию 5, если max_responses не указан в конфиге.

5. В main.cpp нужно собрать полный сценарий работы:

   прочитать документы → построить индекс → прочитать запросы → выполнить поиск → записать ответы.

6. После этого добавить тесты не только на ConverterJSON, но и на InvertedIndex и SearchServer.



То есть структура в целом нормальная, полностью сносить её не нужно.
Но дальше лучше двигаться не в сторону оформления, 
а в сторону реализации базовой логики: индекс, поиск, релевантность, запись ответа.
